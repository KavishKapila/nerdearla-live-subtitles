# --- Core server -------------------------------------------------------
fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
python-dotenv>=1.0,<2.0
numpy>=1.26,<3.0
python-multipart>=0.0.9

# --- Speech-to-text ----------------------------------------------------
faster-whisper>=1.0.3,<2.0

# --- HTTP (used for DeepL + optional scripts) --------------------------
requests>=2.31,<3.0

# --- Firebase ----------------------------------------------------------
firebase-admin>=6.5,<7.0

# --- Optional ----------------------------------------------------------
websockets>=12.0,<14.0